const btn = document.getElementById('sidebar-active-links');

btn.addEventListener('click', function onClick(event) {
  // 👇️ change background color
  document.body.style.backgroundColor = '#f6f9ff';

  
});
